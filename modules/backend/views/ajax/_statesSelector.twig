
<div class="col-lg-3">
    {{ regionsForm.dropDownList('state', regionsForm.stateItems, {
        'id': null,
        'class': 'form-control auto-ajax-select',
        'data-url': path('ajax/statecities'),
        'data-find': '$(this).closest(".col-lg-3").siblings(".cities-selector")'
    }) }}
</div>

<div class="cities-selector">
    {% if regionsForm.state and regionsForm.cities %}
        {% include '@app/modules/backend/views/ajax/_citiesSelector.twig' %}
    {% endif %}
</div>